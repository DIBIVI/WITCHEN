// the character is the target for the camera

var _character_x = obj_character.x;
var _character_y = obj_character.y;

// keep the character in the centre of the camera
var center_x = _character_x - (camera_width*0.5);
var center_y = _character_y - (camera_height*0.5);
